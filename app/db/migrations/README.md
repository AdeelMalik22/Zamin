# Migrations

Alembic is configured from the repository-level [`alembic/`](../../../alembic) directory. Keeping the migration entry point at the project root follows Alembic's standard CLI layout while the ORM metadata remains under `app/db/`.
